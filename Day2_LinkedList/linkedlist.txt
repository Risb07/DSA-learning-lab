it is not lifo not fifo
its strength is to able to operate from any side 

uses Has-a relationaship
linkedlist is a collection of nodes arranged in a sequence manner.

easiest example -
      is a train has a single driver , structure wise evrry boogies is same.

for traversal we have linear linked list 'mostly dynamic  (99.9%)
its strength is its dynamic behaviour 

example - snake game - collection of blocks increasing at runtime. 

---------
Node 

node - a collection of data and reference to another next node itself;

next - a self - referring structure .

it's like a pointer kind which can only refer to its type - 

similarly 
a node can refere to its same data type only.

so how to make a Node- 
      we will create a class 

data unit can be mulitple also 

internal data will have multiple values creating data in real life .

class Node{
      int data ;
      Node next;
      Node(int data ){
            this.data = data ;
            this.next  = null //not refering to anyone ;
      }

} // this is a node 

-------------------------
LinkedList 

A linked list -  has a first node root , head or start. if root lost the entire linkedlist will be deleted or lost .

we store only root addres of list and why root is important -  because in list or array the reference of root via multiple node is maintained , hence root is important.

Dynamic data can be present on different non sequential address and they are connected by links 





## Explain LinkedList in paper pen in interview.(paper pen helps to communicate and explain in better way )
-----------------------------------------------------------------


## an Interview question -  since AGC in java how we have only base root addres of a list  ? 
---------------------------------------------------------------




when we say - Node n = new Node(10);

Node t = root;  //1200 t = 1200 //another ref (alias)

t = t.next // next address t<-2400 moves ahead 

moving from one to another is called traversal , 

Root is at leftmost in a linked list

last or rightmost is the one with next as null. 

. . . . . . . . . . . . . . . . . . . . . . .  . . . . . . . .

example  -    | 1 , &| 2 , &| 3 , &|4 ,  null| 

-> here & indicate address to next element where last element has no address to next 

. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 


root is never created but assigned (1st from either side becomes root)


general  facts-------------------

      xerox is the first company which created photocopy machine.
      because they were the pioneer we named term on them.

---------------------




So as we said Node is a class.
 
steps to create a node..

      All operations would be on basis of roots

      1.create a node
      2.root is assigned never created 

-----------------------
custom methods - 

insert_left

            if(root == null)
            {
            n.next = root;
            root = n ; //root reassigns itself 
            }

root is supposed to left most (first element)

whenever you insert on left side of a root, root reassigns itself to left side element 


insert_Righmost 

property of rightmost element is that it is a " null ", it has no next reference

unidirectional arrow     | 1 , & | -> | 2 , & | -> | 3 , & | -> | 4 , null |


Node n = new Node(data);//creates a node

            if(root==null)
                  root=n;
            else
            {
            Node t = root ;
            Whie(t.next != null) 
                  t=t.next;
            t.next = n ; 
            }

##  interview question - why you would not delete the element on linkedlist 
-------------------------------------------------------




because in java there is AGC automatic garbage collection.

can u delete manually ? 
- no ,its GC's work .


tailing method - 
anchoring the tag  - 

delete right  - 

Node t, t2;
t = t2 =root;

t2.next = null;
probelm is if root is a single node. 
if(root.next == null) //single node.
      root = null ; //manual deletion


8 methods of linkedlist - each have node , t , next .


---------------------------------------------------------
Finding element in LinkedList 

node is a class has data and next , next it is know as self refrring structure. a self referring structure can refer to own type, a node it is refreeing to should be of same type.
we talked about insert left insert right delete left delete right and print .

search- 
insert_after 
insert_before 
delete_before
delete_after
delete_specific

search- 
it is a method, without searching you will not be able to do insert after or  delete_specific. 
searching is a variation of printing . since we have a linear structure you only have option of sequential search (serach data after other ) . 
searching mechanism - it is similar to printing concept 
for searching we need a key. we check every element if data of node is key. 

insert after -


search- 
insert_after 
insert_before 
delete_specific

case 1 if element to be deleted is on root only - 
case 2 if element to be deleted is at last then make its previous element's next as null 
case 3 if element to be deleted is in between then rewrite the next of previous element with the next of current element.



-------------------
Dynamic stack using queue

root becomes tos 

push() - insert_left()
pop() - delete_left() -> tos = null
peek() - tos.data
print_stack() 
stack_empty()

since it is dynamic no empty check , no create stack for it 


## if u are unclear about any question never open ur mouth instead politely ask him that "if your trying to say this" he says yes and then answer 



----------------------------------------

Queue- dynamic version 

enqueue is variation of insert right  -  
if it is a single node -  rear = front = n ;

deque is variation of 
if there is a single node -  hence front = rear = 0 , so we do  - front = rear = null ;

if there is more than one elements(nodes) hence move front and when exactly front = rear  


check what changes were made in first program - to learn things faster- first understand basic things first and see what changes made in that basic code , it will make u rememeber and learn faster 


--------------------------------------
Circular Linked List 

in this list  the last must refer to the first 
last.next  =   root

in case of a single node root and last is itself  

root = last = n;
last.next = root; //next has address of root which is itself 


in case of more than one node  (insert right )

last = next.n
last = n;
last.next=root;


( insert left )

n.next = root;
root =n;
last.next= root;


delete left in circular link list

node t = root;
if(root == last) //single node      
      root =last = null;
else
      root = root.next; make root which is on next of current root 
      last.next = root ; last next must refere to new root


do{
      t =t.next; 
}while(t!=root);
-------------------------------------------------

Doubly linked List 

Traditional and linear linked list can only traverse in one direction, start to end.

Making it slow, specially to travel's last visit 
solution to this is doubly linked List

doubly linkedlist has one root only .


class Node / Dnode (double node)

class Dnode {

}

 . . . . . . . . . . . . . . 
book for data structure 
tunning book - 
 . . . .. . . . . . . . . . .

 you are referring to a node which does not exist.

 insert_left{

      DNode n = new DNode(data);

      if(root = null){
            root = n;
      }else{

            n.right = root;  root.left = n; // linked the left new node and older root node with each other


      }
 }

 ------------------

 delete left 


t = root
root = root.right
root.left = null;

how to know a dnode is single 
if root.left == null and root.right == null;

      then root = null;
else
      root = root.right 
      


------------------------
Since we have two ends no need for t2 to traverse 

----------------------

delete right  -- 

##     interview's faviourite topic to write delete right in doublylinkedlist with paper pen

else{
      Dnode t, t2;
      t =root;
       if(root.right == null )
            root=null;
      else

      {

            while(t.right!=null)
                  t=t.right;
            t2 = t.left;
            t2.right = null;
      }
}

###      after ai - interviews want you to explain code.

reasons for non getting selection ----------------


## interview sees the standard of your code ( tabs - readability , readablility of a variable, well commented code -it  tells interviewer steps are clear to you , )

a senior coder puts logical comments before code.

a lot of companies count number of times you have run your code.

no calls - no profile matched you keep your data in buffer till next profile meets you. 

What is interview ?  - selling yourself 

every coding question is based on some core topic 
example. shortage path algorithm.

lot of interview have started story telling kind of question.

how can a developer take out informatioon and required data for a problem and solve it from an set information.
that is why companies now a days asks questions based on some data and developers to take data from it and resolve the probelm 




microsoft ineterview preperation book -


-----------------------------------------------------




















